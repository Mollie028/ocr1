services:
  - type: web
    name: ocr-whisper-api
    env: python
    plan: hobby # <--- 請再次確認這裡是否已修改為您購買的方案名稱，這仍然很重要！
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ocr_api:app --host 0.0.0.0 --port $PORT # <--- 修改這一行
